apiVersion: v1
kind: Service
metadata:
  name: {{ include "crashchart.fullname" . }}
  labels:
    {{- include "crashchart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "crashchart.selectorLabels" . | nindent 4 }}
